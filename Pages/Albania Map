import React, { useState, useEffect } from 'react';
import { base44 } from '@/api/mockDataClient';
import { useQuery } from '@tanstack/react-query';
import AlbaniaMapSVG, { municipalities } from '../components/map/AlbaniaMapSVG';
import CityModal from '../components/map/CityModal';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Badge } from '@/components/ui/badge';
import { Map, Search, MapPin, Sparkles } from 'lucide-react';
import { Button } from '@/components/ui/button';

export default function AlbaniaMap() {
    const [selectedCity, setSelectedCity] = useState(null);
    const [hoveredCity, setHoveredCity] = useState(null);
    const [searchQuery, setSearchQuery] = useState('');
    const [selectedRegion, setSelectedRegion] = useState('all');
    const [modalOpen, setModalOpen] = useState(false);

    // Fetch wisdom data for cities
    const { data: wisdomData = [] } = useQuery({
        queryKey: ['cityWisdom'],
        queryFn: () => base44.entities.CityWisdom.list(),
        initialData: [],
    });

    const handleCityClick = (city) => {
        setSelectedCity(city);
        setModalOpen(true);
    };

    const getWisdomForCity = (cityName) => {
        return wisdomData.find((w) => w.city_name === cityName);
    };

    const filteredMunicipalities = municipalities.filter((city) => {
        const matchesSearch = city.name.toLowerCase().includes(searchQuery.toLowerCase());
        const matchesRegion = selectedRegion === 'all' || city.region === selectedRegion;
        return matchesSearch && matchesRegion;
    });

    const regionCounts = {
        Veri: municipalities.filter((c) => c.region === 'Veri').length,
        Qendër: municipalities.filter((c) => c.region === 'Qendër').length,
        Jug: municipalities.filter((c) => c.region === 'Jug').length,
    };

    return (
        <div className="min-h-screen py-12">
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                {/* Header */}
                <div className="text-center mb-12">
                    <div className="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-gradient-to-r from-red-100 to-rose-100 dark:from-red-950/50 dark:to-rose-950/50 border-2 border-red-300 dark:border-red-700 mb-4 shadow-lg shadow-red-500/20">
                        <Map className="w-5 h-5 text-red-600 dark:text-red-400 animate-pulse" />
                        <span className="text-sm font-bold text-red-700 dark:text-red-300">
                            Harta Interaktive
                        </span>
                    </div>

                    <h1 className="text-4xl md:text-5xl font-bold mb-4 bg-gradient-to-r from-red-900 via-red-600 to-red-900 dark:from-red-400 dark:via-red-300 dark:to-red-400 bg-clip-text text-transparent drop-shadow-lg">
                        Shqipëria – Harta e Mençurisë
                    </h1>

                    <p className="text-lg text-slate-600 dark:text-slate-400 max-w-3xl mx-auto">
                        Eksploroni fjalë të urta dhe thënie popullore nga të 61 bashkitë e Shqipërisë.
                        Klikoni mbi çdo qytet për të zbuluar mençurinë lokale.
                    </p>
                </div>

                {/* Stats Bar */}
                <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
                    <Card>
                        <CardContent className="p-4 text-center">
                            <div className="text-2xl font-bold text-red-600 dark:text-red-500 mb-1">61</div>
                            <div className="text-sm text-slate-600 dark:text-slate-400">Bashki</div>
                        </CardContent>
                    </Card>
                    <Card>
                        <CardContent className="p-4 text-center">
                            <div className="text-2xl font-bold text-blue-600 dark:text-blue-500 mb-1">{regionCounts.Veri}</div>
                            <div className="text-sm text-slate-600 dark:text-slate-400">Veriu</div>
                        </CardContent>
                    </Card>
                    <Card>
                        <CardContent className="p-4 text-center">
                            <div className="text-2xl font-bold text-amber-600 dark:text-amber-500 mb-1">{regionCounts.Qendër}</div>
                            <div className="text-sm text-slate-600 dark:text-slate-400">Qendra</div>
                        </CardContent>
                    </Card>
                    <Card>
                        <CardContent className="p-4 text-center">
                            <div className="text-2xl font-bold text-red-600 dark:text-red-500 mb-1">{regionCounts.Jug}</div>
                            <div className="text-sm text-slate-600 dark:text-slate-400">Jugu</div>
                        </CardContent>
                    </Card>
                </div>

                <div className="grid lg:grid-cols-3 gap-8">
                    {/* Map Section */}
                    <div className="lg:col-span-2">
                        <Card className="overflow-hidden border-2">
                            <CardHeader className="bg-gradient-to-r from-red-50 to-slate-50 dark:from-red-950/20 dark:to-slate-950/20 border-b">
                                <CardTitle className="flex items-center gap-2">
                                    <MapPin className="w-5 h-5 text-red-600" />
                                    Harta e Shqipërisë
                                </CardTitle>
                            </CardHeader>
                            <CardContent className="p-8 bg-slate-50 dark:bg-slate-900/50">
                                <AlbaniaMapSVG
                                    onCityClick={handleCityClick}
                                    hoveredCity={hoveredCity}
                                    selectedCity={selectedCity?.name}
                                />
                                <div className="mt-4 text-center">
                                    <p className="text-sm text-slate-600 dark:text-slate-400">
                                        💡 Klikoni mbi qytetet për të parë fjalë të urta
                                    </p>
                                </div>
                            </CardContent>
                        </Card>
                    </div>

                    {/* Sidebar */}
                    <div className="space-y-6">
                        {/* Search */}
                        <Card>
                            <CardHeader>
                                <CardTitle className="text-lg">Kërko Qytet</CardTitle>
                            </CardHeader>
                            <CardContent className="space-y-4">
                                <div className="relative">
                                    <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400" />
                                    <Input
                                        placeholder="Shkruani emrin e qytetit..."
                                        value={searchQuery}
                                        onChange={(e) => setSearchQuery(e.target.value)}
                                        className="pl-10"
                                    />
                                </div>

                                {/* Region Filter */}
                                <div>
                                    <p className="text-sm font-medium mb-2 text-slate-700 dark:text-slate-300">Filtro sipas rajonit:</p>
                                    <div className="flex flex-wrap gap-2">
                                        <Badge
                                            variant={selectedRegion === 'all' ? 'default' : 'outline'}
                                            className="cursor-pointer"
                                            onClick={() => setSelectedRegion('all')}
                                        >
                                            Të gjitha
                                        </Badge>
                                        <Badge
                                            variant={selectedRegion === 'Veri' ? 'default' : 'outline'}
                                            className="cursor-pointer bg-blue-100 text-blue-800 dark:bg-blue-950/50 dark:text-blue-400"
                                            onClick={() => setSelectedRegion('Veri')}
                                        >
                                            Veriu
                                        </Badge>
                                        <Badge
                                            variant={selectedRegion === 'Qendër' ? 'default' : 'outline'}
                                            className="cursor-pointer bg-amber-100 text-amber-800 dark:bg-amber-950/50 dark:text-amber-400"
                                            onClick={() => setSelectedRegion('Qendër')}
                                        >
                                            Qendra
                                        </Badge>
                                        <Badge
                                            variant={selectedRegion === 'Jug' ? 'default' : 'outline'}
                                            className="cursor-pointer bg-red-100 text-red-800 dark:bg-red-950/50 dark:text-red-400"
                                            onClick={() => setSelectedRegion('Jug')}
                                        >
                                            Jugu
                                        </Badge>
                                    </div>
                                </div>
                            </CardContent>
                        </Card>

                        {/* City List */}
                        <Card>
                            <CardHeader>
                                <CardTitle className="text-lg flex items-center gap-2">
                                    <Sparkles className="w-5 h-5 text-amber-500" />
                                    Lista e Qyteteve
                                </CardTitle>
                            </CardHeader>
                            <CardContent>
                                <div className="space-y-2 max-h-96 overflow-y-auto">
                                    {filteredMunicipalities.map((city) => {
                                        const regionColors = {
